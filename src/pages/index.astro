---
import Layout from "../layouts/Layout.astro";

const APP_URL = "https://app.worshipmetrics.com";
const SITE_URL = "https://worshipmetrics.com";
const R2      = "https://pub-a4845709a76642e7943bcb8b80677531.r2.dev/main";
const R2NEW   = `${R2}/homepage/new%20images`;
const SIGNAGE_EXAMPLE_URL = "https://app.worshipmetrics.com/tv/00000000-0000-4000-8000-00000000de10";
const IOS_APP_URL = "https://apps.apple.com/us/app/worship-metrics/id6754773170";
const ANDROID_APP_URL = "https://play.google.com/store/apps/details?id=com.worshipmetrics.kiosk";
const APP_STORE_BADGE = "https://pub-a4845709a76642e7943bcb8b80677531.r2.dev/main/homepage/Download_on_the_App_Store_Badge.svg.png";
const GOOGLE_PLAY_BADGE = "https://pub-a4845709a76642e7943bcb8b80677531.r2.dev/main/homepage/Google_Play_Store_badge_EN.svg.png";
const PAUL_HEADSHOT = `${R2}/paul.jpg`;
const ANDREW_HEADSHOT = `${R2}/Andrew.jpg`;

const structuredData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "WorshipMetrics",
  applicationCategory: "BusinessApplication",
  operatingSystem: "Web, iOS, Android",
  url: SITE_URL,
  image: `${R2}/homepage/WorshipMetrics%20Hero%20New.jpg`,
  description: "Congregation technology for AV monitoring, automatic attendance, free digital signage, and all-week engagement in one platform.",
  offers: {
    "@type": "Offer",
    price: "0",
    priceCurrency: "USD",
    availability: "https://schema.org/InStock",
    url: `${SITE_URL}/pricing`,
  },
};
---

<Layout
  title="Church Attendance Tracking Software and Digital Engagement"
  description="WorshipMetrics helps churches track attendance, capture visitors, monitor livestream health, manage digital signage, and turn online engagement into follow-up."
  canonical="/"
  ogImage={`${R2}/homepage/WorshipMetrics%20Hero%20New.jpg`}
  structuredData={structuredData}
>
<style>
  /* ── Shared rhythm ─────────────────────────────────── */
  .section      { padding: 5rem 1.5rem; }
  .section-alt  { background: var(--bg-soft); }
  .inner        { max-width: 72rem; margin: 0 auto; }
  .inner-md     { max-width: 56rem; margin: 0 auto; }
  .inner-sm     { max-width: 44rem; margin: 0 auto; }

  .eyebrow-center { text-align: center; margin-bottom: 0.4rem; }
  .h2-center {
    font-size: clamp(1.7rem, 3vw, 2.5rem); font-weight: 800;
    color: var(--text); text-align: center; line-height: 1.15; margin-bottom: 0.75rem;
  }
  .sub-center {
    text-align: center; color: var(--text-muted); font-size: 1rem;
    max-width: 42rem; margin: 0 auto 3rem; line-height: 1.72;
  }

  /* ── HERO ──────────────────────────────────────────── */
  .hero {
    padding: 5.5rem 1.5rem 0;
    text-align: center;
    background: linear-gradient(175deg, #eef1fd 0%, #ffffff 60%);
    border-bottom: 1px solid var(--border);
    overflow: hidden;
  }
  .hero-badge {
    display: inline-flex; align-items: center; gap: 0.4rem;
    background: #fff; border: 1.5px solid var(--primary-border);
    color: var(--primary); border-radius: 999px;
    font-size: 0.7rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase;
    padding: 0.28rem 0.9rem; margin-bottom: 1.5rem;
    box-shadow: 0 2px 8px rgba(67,97,238,0.1);
  }
  .hero h1 {
    font-size: clamp(2.4rem, 5.5vw, 4.25rem); font-weight: 800;
    line-height: 1.08; letter-spacing: -0.03em; color: var(--text);
    margin-bottom: 1.25rem;
  }
  .hero h1 em { font-style: normal; color: var(--primary); }
  .hero-sub {
    font-size: 1.1rem; color: var(--text-soft); max-width: 42rem;
    margin: 0 auto 2.25rem; line-height: 1.75;
  }
  .hero-ctas { display: flex; flex-wrap: wrap; justify-content: center; gap: 0.75rem; margin-bottom: 0.9rem; }
  .hero-fine  { font-size: 0.8rem; color: var(--text-muted); margin-bottom: 3rem; }

  .hero-app-band {
    max-width: 48rem; margin: 0 auto 3rem;
    display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 1.25rem;
    align-items: center;
    padding: 1.35rem 1.5rem;
    border-radius: 1.5rem;
    border: 1px solid rgba(129, 140, 248, 0.16);
    background: linear-gradient(135deg, #0f172a 0%, #312e81 55%, #0f172a 100%);
    box-shadow: 0 20px 50px rgba(15,26,46,0.14);
    text-align: left;
  }
  .hero-app-copy p:first-child {
    font-size: 0.68rem; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase;
    color: rgba(165, 180, 252, 0.94); margin-bottom: 0.45rem;
  }
  .hero-app-copy h2 {
    font-size: clamp(1.2rem, 2.4vw, 1.7rem); font-weight: 800;
    color: #fff; line-height: 1.15; margin-bottom: 0.5rem;
  }
  .hero-app-copy p:last-child {
    font-size: 0.92rem; line-height: 1.7; color: rgba(226,232,240,0.82);
  }
  .hero-app-badges {
    display: flex; flex-direction: column; gap: 0.75rem; align-items: flex-end;
  }
  .hero-app-badges a { display: inline-flex; transition: opacity 0.15s, transform 0.15s; }
  .hero-app-badges a:hover { opacity: 0.92; transform: translateY(-1px); }
  .hero-app-badges img { height: 3.5rem; width: auto; display: block; }
  @media (max-width: 760px) {
    .hero-app-band { grid-template-columns: 1fr; text-align: center; }
    .hero-app-badges { align-items: center; }
  }

  .hero-img-wrap {
    max-width: 64rem; margin: 0 auto;
    border-radius: 1rem 1rem 0 0; overflow: hidden;
    border: 1px solid var(--border); border-bottom: none;
    box-shadow: 0 -4px 40px rgba(67,97,238,0.06), 0 24px 64px rgba(15,26,46,0.1);
  }
  .hero-img-wrap img { display: block; width: 100%; height: auto; }

  .social-proof-bar {
    background: #0f1a2e; color: rgba(255,255,255,0.86);
    text-align: center; padding: 0.95rem 1.5rem;
    font-size: 0.85rem; font-weight: 700; letter-spacing: 0.02em;
  }

  /* ── PAIN → PROMISE ────────────────────────────────── */
  .pain-grid {
    display: grid; grid-template-columns: repeat(2,1fr); gap: 1.5rem;
  }
  @media (max-width: 860px) { .pain-grid { grid-template-columns: 1fr; max-width: 36rem; margin: 0 auto; } }

  .pain-card {
    border: 1.5px solid var(--border); border-radius: var(--radius-card);
    background: #fff; padding: 1.75rem; position: relative; overflow: hidden;
    transition: box-shadow 0.2s, border-color 0.2s, transform 0.2s;
  }
  .pain-card:hover { border-color: var(--primary-border); box-shadow: 0 8px 28px rgba(67,97,238,0.09); transform: translateY(-2px); }
  .pain-before {
    font-size: 0.75rem; font-weight: 700; color: #dc2626;
    letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 0.5rem;
    display: flex; align-items: center; gap: 0.35rem;
  }
  .pain-before::before { content: '✕'; font-size: 0.7rem; }
  .pain-after {
    font-size: 0.75rem; font-weight: 700; color: #16a34a;
    letter-spacing: 0.08em; text-transform: uppercase; margin: 1rem 0 0.5rem;
    display: flex; align-items: center; gap: 0.35rem;
  }
  .pain-after::before { content: '✓'; font-size: 0.7rem; }
  .pain-problem { font-size: 0.925rem; color: var(--text-soft); line-height: 1.6; font-style: italic; }
  .pain-solution { font-size: 0.925rem; color: var(--text); font-weight: 600; line-height: 1.6; }
  .pain-icon {
    width: 2.75rem; height: 2.75rem; border-radius: 0.625rem;
    background: var(--primary-light); display: flex; align-items: center; justify-content: center;
    margin-bottom: 1rem;
  }
  .pain-icon svg { width: 1.3rem; height: 1.3rem; color: var(--primary); }

  /* ── ECOSYSTEM GRID ────────────────────────────────── */
  .eco-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-top: 2.5rem;
  }
  @media (max-width: 900px) { .eco-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 560px) { .eco-grid { grid-template-columns: 1fr; } }

  .eco-card {
    background: #fff; border: 1.5px solid var(--border);
    border-radius: var(--radius-card); padding: 1.5rem;
    text-decoration: none; display: flex; flex-direction: column; gap: 0.5rem;
    transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s;
  }
  .eco-card:hover { border-color: var(--primary-border); box-shadow: 0 8px 28px rgba(67,97,238,0.09); transform: translateY(-2px); }
  .eco-card-media {
    aspect-ratio: 16 / 10; overflow: hidden; border-radius: 0.9rem;
    border: 1px solid var(--border); margin-bottom: 0.4rem;
    background: #eef2ff;
  }
  .eco-card-media img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .eco-label {
    font-size: 0.65rem; font-weight: 700; letter-spacing: 0.12em;
    text-transform: uppercase; color: var(--text-muted);
  }
  .eco-title { font-size: 1rem; font-weight: 700; color: var(--text); }
  .eco-desc  { font-size: 0.825rem; color: var(--text-soft); line-height: 1.6; flex: 1; }
  .eco-link  { font-size: 0.8rem; font-weight: 700; color: var(--primary); margin-top: 0.25rem; }
  .eco-badge {
    display: inline-block; font-size: 0.65rem; font-weight: 700;
    padding: 0.2rem 0.55rem; border-radius: 999px;
    background: #dcfce7; color: #15803d; align-self: flex-start;
  }

  /* ── SPLIT SECTION ─────────────────────────────────── */
  .split { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center; }
  @media (max-width: 768px) { .split { grid-template-columns: 1fr; gap: 2.5rem; } }
  .split-img {
    border-radius: 0.875rem; overflow: hidden;
    border: 1px solid var(--border);
    box-shadow: 0 12px 40px rgba(15,26,46,0.09);
  }
  .split-img img { display: block; width: 100%; height: auto; }
  .split-text h2 {
    font-size: clamp(1.5rem, 2.5vw, 2rem); font-weight: 800;
    color: var(--text); line-height: 1.2; margin-bottom: 0.75rem;
  }
  .split-text p   { color: var(--text-soft); line-height: 1.77; margin-bottom: 1.25rem; font-size: 0.95rem; }
  .bullets { list-style: none; padding: 0; margin: 0 0 1.5rem; display: flex; flex-direction: column; gap: 0.6rem; }
  .bullets li {
    display: flex; align-items: flex-start; gap: 0.65rem;
    font-size: 0.9rem; color: var(--text-soft);
  }
  .bullets li::before {
    content: ''; flex-shrink: 0; width: 6px; height: 6px;
    border-radius: 50%; background: var(--primary); margin-top: 0.5rem;
  }

  /* ── AV / SIGNAGE highlight band ───────────────────── */
  .highlight-band {
    background: var(--primary); border-radius: 1.25rem;
    padding: 2.5rem; display: flex; align-items: center; gap: 2rem;
    margin-bottom: 1.5rem;
  }
  @media (max-width: 640px) { .highlight-band { flex-direction: column; text-align: center; } }
  .highlight-band-icon {
    flex-shrink: 0; width: 3.5rem; height: 3.5rem;
    background: rgba(255,255,255,0.15); border-radius: 0.875rem;
    display: flex; align-items: center; justify-content: center;
  }
  .highlight-band-icon svg { width: 1.75rem; height: 1.75rem; color: #fff; }
  .highlight-band h3 { font-size: 1.1rem; font-weight: 800; color: #fff; margin-bottom: 0.35rem; }
  .highlight-band p  { font-size: 0.875rem; color: rgba(255,255,255,0.8); line-height: 1.65; margin: 0; }

  /* ── VOLUNTEER ROLES ───────────────────────────────── */
  .roles-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 1rem; margin-top: 2.5rem; }
  @media (max-width: 900px) { .roles-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 520px) { .roles-grid { grid-template-columns: 1fr; } }

  .role-card {
    background: #fff; border: 1.5px solid var(--border);
    border-radius: var(--radius-card); padding: 1.4rem;
    transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s;
  }
  .role-card:hover { border-color: var(--primary-border); box-shadow: 0 8px 24px rgba(67,97,238,0.08); transform: translateY(-2px); }
  .role-emoji { font-size: 1.75rem; margin-bottom: 0.75rem; }
  .role-name { font-size: 0.875rem; font-weight: 800; color: var(--text); margin-bottom: 0.2rem; }
  .role-job  { font-size: 0.7rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--primary); margin-bottom: 0.5rem; }
  .role-desc { font-size: 0.825rem; color: var(--text-soft); line-height: 1.6; }

  /* ── ORIGIN / MISSION ──────────────────────────────── */
  .mission-band {
    background: var(--text); border-radius: 1.5rem;
    padding: 3.5rem; text-align: center;
  }
  .mission-band h2 { font-size: clamp(1.6rem, 2.5vw, 2.2rem); font-weight: 800; color: #fff; margin-bottom: 0.75rem; line-height: 1.2; }
  .mission-band p  { color: rgba(255,255,255,0.7); font-size: 1rem; line-height: 1.75; max-width: 42rem; margin: 0 auto 2rem; }
  .founders { display: flex; justify-content: center; gap: 2rem; flex-wrap: wrap; margin-bottom: 2rem; }
  .founder  {
    display: flex; flex-direction: column; align-items: center; gap: 0.5rem;
    width: 9rem;
  }
  .founder-avatar {
    width: 5rem; height: 5rem; border-radius: 50%;
    background: rgba(255,255,255,0.12); border: 2px solid rgba(255,255,255,0.2);
    overflow: hidden; display: flex; align-items: center; justify-content: center;
    color: rgba(255,255,255,0.4); font-size: 1.5rem;
  }
  .founder-avatar img { width: 100%; height: 100%; object-fit: cover; }
  .founder-name  { font-size: 0.875rem; font-weight: 700; color: #fff; }
  .founder-title { font-size: 0.75rem; color: rgba(255,255,255,0.55); text-align: center; }

  /* ── LUMINOS BAND ──────────────────────────────────── */
  .luminos-band {
    background: linear-gradient(135deg, #f0f3fb 0%, #e8ecff 100%);
    border: 1.5px solid var(--primary-border); border-radius: 1.25rem;
    padding: 2.5rem; display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; align-items: center;
  }
  @media (max-width: 640px) { .luminos-band { grid-template-columns: 1fr; } }
  .luminos-tag { font-size: 0.65rem; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: var(--primary); margin-bottom: 0.5rem; }
  .luminos-band h3 { font-size: 1.4rem; font-weight: 800; color: var(--text); margin-bottom: 0.6rem; line-height: 1.2; }
  .luminos-band p  { font-size: 0.9rem; color: var(--text-soft); line-height: 1.7; margin-bottom: 1.25rem; }
  .luminos-prompts { display: flex; flex-direction: column; gap: 0.5rem; }
  .luminos-prompt {
    background: #fff; border: 1px solid var(--border);
    border-radius: 0.625rem; padding: 0.65rem 0.9rem;
    font-size: 0.825rem; color: var(--text-soft); font-style: italic;
  }
  .luminos-prompt span { font-style: normal; font-weight: 700; color: var(--primary); }

  /* ── STEPS ─────────────────────────────────────────── */
  .steps { display: flex; flex-direction: column; gap: 1.75rem; max-width: 40rem; margin: 0 auto; }
  .step  { display: flex; gap: 1.25rem; align-items: flex-start; }
  .step-num {
    flex-shrink: 0; width: 2.5rem; height: 2.5rem; border-radius: 50%;
    background: var(--primary); display: flex; align-items: center; justify-content: center;
    font-weight: 800; font-size: 0.9rem; color: #fff;
    box-shadow: 0 4px 12px rgba(67,97,238,0.3);
  }
  .step-body h3 { font-size: 1rem; font-weight: 700; color: var(--text); margin-bottom: 0.2rem; }
  .step-body p  { font-size: 0.875rem; color: var(--text-soft); line-height: 1.65; }

  /* ── CTA BAND ──────────────────────────────────────── */
  .cta-band { background: var(--primary); padding: 5.5rem 1.5rem; text-align: center; }
  .cta-band h2 { font-size: clamp(1.9rem, 3.5vw, 2.75rem); font-weight: 800; color: #fff; margin-bottom: 0.75rem; line-height: 1.15; }
  .cta-band p  { color: rgba(255,255,255,0.8); font-size: 1.05rem; margin-bottom: 2.25rem; max-width: 36rem; margin-left: auto; margin-right: auto; }
  .cta-btns { display: flex; flex-wrap: wrap; justify-content: center; gap: 0.75rem; margin-bottom: 1.25rem; }
  .btn-white {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 0.75rem 1.75rem; border-radius: var(--radius-btn);
    background: #fff; color: var(--primary); font-weight: 700; font-size: 0.9rem;
    text-decoration: none; box-shadow: 0 4px 16px rgba(0,0,0,0.15);
    transition: box-shadow 0.15s, transform 0.15s;
  }
  .btn-white:hover { box-shadow: 0 6px 24px rgba(0,0,0,0.2); transform: translateY(-1px); }
  .btn-ghost-white {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 0.75rem 1.75rem; border-radius: var(--radius-btn);
    border: 2px solid rgba(255,255,255,0.45); color: #fff; background: transparent;
    font-weight: 700; font-size: 0.9rem; text-decoration: none;
    transition: background 0.15s, border-color 0.15s;
  }
  .btn-ghost-white:hover { background: rgba(255,255,255,0.12); border-color: #fff; }
  .cta-note { font-size: 0.82rem; color: rgba(255,255,255,0.55); }

  .testimonial-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 1rem; margin-top: 2.5rem; }
  @media (max-width: 900px) { .testimonial-grid { grid-template-columns: 1fr; } }
  .testimonial-card {
    background: #fff; border: 1.5px solid var(--border); border-radius: var(--radius-card);
    padding: 1.5rem; box-shadow: 0 10px 24px rgba(15,26,46,0.06);
  }
  .testimonial-stars { color: #f59e0b; font-size: 0.9rem; letter-spacing: 0.12em; font-weight: 800; margin-bottom: 0.8rem; }
  .testimonial-quote { color: var(--text); line-height: 1.72; font-size: 0.92rem; margin-bottom: 1rem; }
  .testimonial-author { color: var(--text-muted); font-size: 0.8rem; font-weight: 700; }
</style>

<!-- ═══════════════════════════════════════════════════
     HERO
════════════════════════════════════════════════════ -->
<section class="hero">
  <div class="inner-sm">
    <div class="hero-badge">
      The Complete Congregation Technology Platform
    </div>
    <h1>
      Where technology<br/>
      <em>serves the mission.</em>
    </h1>
    <p class="hero-sub">
      Reach every person. Every screen. Every service. — AV monitoring, automatic attendance, free digital signage, and tools that connect your congregation all week long.
    </p>
    <div class="hero-ctas">
      <a href={`${APP_URL}/sign-up`} class="btn-primary">Start free — no credit card</a>
      <a href="/tech-manager" class="btn-outline">See Tech Manager</a>
    </div>
    <p class="hero-fine">Free forever. Upgrade when you're ready.</p>

    <div class="hero-app-band">
      <div class="hero-app-copy">
        <p>iOS + Android apps</p>
        <h2>Download WorshipMetrics and keep your congregation workflow in your pocket.</h2>
        <p>
          Free today for check-ins, attendance workflows, and ministry updates from anywhere.
        </p>
      </div>
      <div class="hero-app-badges">
        <a href={IOS_APP_URL} aria-label="Download WorshipMetrics on the App Store">
          <img src={APP_STORE_BADGE} alt="Download on the App Store" loading="eager" decoding="async" />
        </a>
        <a href={ANDROID_APP_URL} aria-label="Get WorshipMetrics on Google Play">
          <img src={GOOGLE_PLAY_BADGE} alt="Get it on Google Play" loading="eager" decoding="async" />
        </a>
      </div>
    </div>
  </div>

  <div class="hero-img-wrap">
    <img
      src={`${R2}/homepage/WorshipMetrics%20Hero%20New.jpg`}
      alt="WorshipMetrics — the church technology partner"
      loading="eager" width="1280" height="720"
    />
  </div>
</section>

<section class="social-proof-bar">
  Trusted by congregations of every size — from 50 seats to 5,000.
</section>

<!-- ═══════════════════════════════════════════════════
     PAIN → PROMISE
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <p class="eyebrow eyebrow-center">The challenge</p>
    <h2 class="h2-center">Every congregation is doing technology.<br/>Most are doing it alone.</h2>
    <p class="sub-center">We built WorshipMetrics because we lived these problems firsthand.</p>

    <div class="pain-grid">
      {[
        {
          icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M12 18v-5.25m0 0a6.01 6.01 0 001.5-.189m-1.5.189a6.01 6.01 0 01-1.5-.189m3.75 7.478a12.06 12.06 0 01-4.5 0m3.75 2.383a14.406 14.406 0 01-3 0M14.25 18v-.192c0-.983.658-1.823 1.508-2.316a7.5 7.5 0 10-7.517 0c.85.493 1.509 1.333 1.509 2.316V18"/>`,
          before: "Your stream goes down unnoticed",
          problem: "Twelve minutes in someone texts you. Half your online congregation already gave up.",
          after: "What WorshipMetrics does",
          solution: "Real-time AV monitoring watches your stream, audio, cameras, and displays continuously. Your team gets an instant alert the moment something goes wrong — before anyone in the congregation notices.",
        },
        {
          icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M3.75 12h16.5m-16.5 3.75h16.5M3.75 19.5h16.5M5.625 4.5h12.75a1.875 1.875 0 010 3.75H5.625a1.875 1.875 0 010-3.75z"/>`,
          before: "You're still counting on paper",
          problem: "Someone tallies attendance on a notepad. It maybe gets entered into a spreadsheet. Maybe.",
          after: "What WorshipMetrics does",
          solution: "Auto-Count does the heavy lifting. One tap or fully automatic — your attendance is recorded, stored, and ready for Luminos AI to turn into pastoral insight before Monday morning.",
        },
        {
          icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M18 18.72a9.094 9.094 0 003.741-.479 3 3 0 00-4.682-2.72m.94 3.198l.001.031c0 .225-.012.447-.037.666A11.944 11.944 0 0112 21c-2.17 0-4.207-.576-5.963-1.584A6.062 6.062 0 016 18.719m12 0a5.971 5.971 0 00-.941-3.197m0 0A5.995 5.995 0 0012 12.75a5.995 5.995 0 00-5.058 2.772m0 0a3 3 0 00-4.681 2.72 8.986 8.986 0 003.74.477m.94-3.197a5.971 5.971 0 00-.94 3.197M15 6.75a3 3 0 11-6 0 3 3 0 016 0zm6 3a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0zm-13.5 0a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0z"/>`,
          before: "Online viewers are invisible",
          problem: "You get a view count. You have no idea who they are, whether they came back, or how to reach them.",
          after: "What WorshipMetrics does",
          solution: "Digital engagement tools capture online visitors, route prayer requests, and turn anonymous viewership into real relationships with real follow-up.",
        },
        {
          icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M2.25 4.5A2.25 2.25 0 014.5 2.25h15A2.25 2.25 0 0121.75 4.5v11.25A2.25 2.25 0 0119.5 18h-6l-3.75 3.75V18h-5.25A2.25 2.25 0 012.25 15.75V4.5z"/>`,
          before: "Your lobby screen hasn't been updated in weeks",
          problem: "Someone has to walk to a computer in the building, open a file, and remember the password.",
          after: "What WorshipMetrics does",
          solution: "Free digital signage. Update every screen in your building from your phone, from anywhere, right now. No hardware required. No monthly fees. Ever.",
        },
      ].map(({ icon, before, problem, after, solution }) => (
        <div class="pain-card">
          <div class="pain-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" aria-hidden="true" set:html={icon}/>
          </div>
          <div class="pain-before">{before}</div>
          <p class="pain-problem">"{problem}"</p>
          <div class="pain-after">{after}</div>
          <p class="pain-solution">{solution}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     AV MONITORING
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <div class="split">
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">AV Monitoring</p>
        <h2>We'll tell you when your stream is down<br/>before your congregation does.</h2>
        <p>Sunday morning should not start with a text from a concerned member. The WorshipMetrics platform watches your audio, video, cameras, and displays in real time — so your team knows the moment something goes wrong, not twelve minutes after.</p>
        <ul class="bullets">
          <li>Real-time stream, audio, and display monitoring</li>
          <li>Instant alerts to your team's phones</li>
          <li>Remote PTZ camera control from anywhere in the world</li>
          <li>Remote OBS and vMix control — run a great service without being in the building</li>
          <li>Tech Manager maps your AV system, monitors every device, and gives Luminos what it needs to diagnose problems fast</li>
        </ul>
        <p style="font-size:0.875rem;color:var(--text-muted);font-style:italic;">So nothing gets in the way of the mission.</p>
        <div style="display:flex;flex-wrap:wrap;gap:0.75rem;">
          <a href="/av-monitoring-for-houses-of-worship" class="btn-primary">See AV monitoring</a>
          <a href="/tech-manager" class="btn-outline">See Tech Manager</a>
        </div>
      </div>
      <div class="split-img">
        <img
          src={`${R2NEW}/Stream%20Health.jpg`}
          alt="WorshipMetrics AV monitoring dashboard"
          loading="lazy" width="800" height="540"
        />
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     FREE WORSHIP ATTENDANCE
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner">
    <div class="split">
      <div class="split-img">
        <img
          src={`${R2}/tablet/New%20Worship%20Metrics%20Tablet%20Hero.jpg`}
          alt="Free worship attendance workflows on the WorshipMetrics tablet"
          loading="lazy" width="800" height="540"
        />
      </div>
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">Free Worship Attendance</p>
        <h2>Free worship attendance.<br/>Because every congregation should know who was in the room.</h2>
        <p>Track worship attendance without a credit card, without a complicated setup, and without waiting for a future budget cycle. Start free, keep your history, and give your team a better weekly attendance rhythm right away.</p>
        <ul class="bullets">
          <li>Works with phones, tablets, and the devices your team already has</li>
          <li>Historical records and reporting from the beginning</li>
          <li>One-tap counting or fully automatic with the Hub</li>
          <li>Grow into seat templates, people workflows, and automation later</li>
        </ul>
        <div style="display:flex;flex-wrap:wrap;gap:0.75rem;">
          <a href="/free-church-attendance-software" class="btn-primary">Get free attendance</a>
          <a href="/platform/attendance" class="btn-outline">See attendance workflows</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     ONLINE VIEWERSHIP & ENGAGEMENT
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <div class="split">
      <div class="split-img">
        <img
          src={`${R2}/homepage/Online%20Attendance%20Older%20Woman.jpg`}
          alt="Online congregation engagement through WorshipMetrics"
          loading="lazy" width="800" height="540"
        />
      </div>
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">Online Viewership & Engagement</p>
        <h2>Your online congregation deserves<br/>more than a view count.</h2>
        <p>Every person watching your stream is a real person with a real need. WorshipMetrics captures online visitors, routes prayer requests, and gives your team a clear path to follow up — turning passive viewership into active community membership.</p>
        <ul class="bullets">
          <li>Online visitor capture with next-step prompts</li>
          <li>Prayer request routing to the right team member</li>
          <li>Return viewer tracking — know who keeps coming back</li>
          <li>YouTube chat sync — someone is always in the room welcoming viewers</li>
        </ul>
        <div style="display:flex;flex-wrap:wrap;gap:0.75rem;">
          <a href="/solutions/digital-engagement" class="btn-primary">See engagement tools</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     FREE CHURCH DIGITAL SIGNAGE
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner">
    <div class="split">
      <div class="split-img">
        <img
          src={`${R2NEW}/Worship%20Digital%20Signage%20System.jpg`}
          alt="Free church digital signage layout on a lobby display"
          loading="lazy" width="800" height="540"
        />
      </div>
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">Free Church Digital Signage</p>
        <h2>Your lobby TV deserves better<br/>than a blank screen.</h2>
        <p>Start with 40+ beautiful templates for every major Christian holiday and year-round ministry use. Fill out your Brand Kit once, put the link on any smart TV, and manage your signage remotely from anywhere in the world. Free for every congregation, no Hub required.</p>
        <ul class="bullets">
          <li>40+ starting templates for Christmas, Easter, Good Friday, VBS, and more</li>
          <li>Brand Kit once — colors, logo, and service details applied automatically</li>
          <li>Full Screen and L-Bar layouts for the signage churches actually use most</li>
          <li>Set the TV link once, then manage signage remotely for the future</li>
          <li>Unlimited screens on paid plans — 1 screen free forever</li>
        </ul>
        <div style="display:flex;flex-wrap:wrap;gap:0.75rem;">
          <a href="/signage-display" class="btn-primary">Get free signage</a>
          <a href={SIGNAGE_EXAMPLE_URL} target="_blank" rel="noopener noreferrer" class="btn-outline">See a live example</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     WORSHIP CLIPS
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <div class="split">
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">Worship Clips</p>
        <h2>Your best moments are already<br/>in the cloud before the last car leaves.</h2>
        <p>Every service has a moment worth sharing. WorshipMetrics automatically identifies, clips, and formats your best sermon moments for Reels, Shorts, TikTok, and Facebook — no editor, no extra software, no Monday morning video project.</p>
        <ul class="bullets">
          <li>AI clip detection — key quotes, worship peaks, sermon highlights</li>
          <li>Auto-formatted for every major social platform</li>
          <li>Podcast audio extracted automatically from the sermon segment</li>
          <li>Staff approves clips from the dashboard — nothing posts without your sign-off</li>
        </ul>
        <a href="/solutions/worship-clips" class="btn-primary">See Worship Clips</a>
      </div>
      <div class="split-img">
        <img
          src={`${R2}/homepage/Worship%20Clips%20Hero%202.jpg`}
          alt="Worship Clips creating social content from sermons"
          loading="lazy" width="800" height="540"
        />
      </div>
    </div>
  </div>
</section>

<section class="section section-alt">
  <div class="inner">
    <p class="eyebrow eyebrow-center">Popular Guides</p>
    <h2 class="h2-center">Explore the workflows congregations search for most.</h2>
    <p class="sub-center">These focused guides help explain where WorshipMetrics fits before you dive into the full platform.</p>
    <div class="eco-grid">
      {[
        { href: "/church-attendance-tracking-software", title: "Congregation attendance tracking software", desc: "Track in-person and online attendance, seat counts, and follow-up signals in one workflow." },
        { href: "/church-visitor-follow-up-software", title: "Visitor follow-up software", desc: "Capture guests, route prayer requests, and organize follow-up without losing context." },
        { href: "/church-check-in-tablet", title: "Check-in tablet", desc: "See how a dedicated tablet helps welcome desks, guest capture, and volunteer attendance workflows." },
        { href: "/church-digital-signage-software", title: "Digital signage software", desc: "Manage welcome screens, lobby displays, and announcements from one connected system." },
        { href: "/av-monitoring-for-houses-of-worship", title: "AV monitoring for houses of worship", desc: "Keep your stream, cameras, and displays healthy every service — and know instantly when something goes wrong." },
        { href: "/tech-manager", title: "Tech Manager", desc: "Map your AV system, monitor OBS health, and give Luminos better diagnostic context." },
      ].map(({ href, title, desc }) => (
        <a href={href} class="eco-card">
          <div class="eco-label">Guide</div>
          <div class="eco-title">{title}</div>
          <div class="eco-desc">{desc}</div>
          <div class="eco-link">Read guide →</div>
        </a>
      ))}
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     HUB
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <div class="split">
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">The Hub</p>
        <h2>One device. Your entire congregation, connected.</h2>
        <p>The Worship Metrics Hub is a cloud-connected appliance that plugs into your existing setup and runs your entire Established plan automatically — no computer, no configuration, no volunteer overhead.</p>
        <ul class="bullets">
          <li>Fully automatic attendance — your camera counts your congregation every week</li>
          <li>AV health monitoring — stream, audio, video, and display status in real time</li>
          <li>Remote PTZ camera control — pan, tilt, zoom from anywhere in the world</li>
          <li>Remote OBS and vMix control — run a great service without being in the building</li>
          <li>Auto video upload — sermon clips in the cloud before the last car leaves the parking lot</li>
          <li>Digital signage — every display managed from your phone</li>
        </ul>
        <p style="font-size:0.85rem;color:var(--text-muted);margin-bottom:1rem;">No subscription required. One-time purchase. Plug it in and it works.</p>
        <div style="display:flex;flex-wrap:wrap;gap:0.75rem;">
          <a href="/appliance" class="btn-primary">See the Hub — $499</a>
          <a href="/pricing" class="btn-outline">Compare plans</a>
        </div>
      </div>
      <div class="split-img">
        <img
          src={`${R2NEW}/Stream%20Health.jpg`}
          alt="WorshipMetrics AV monitoring and stream health dashboard"
          loading="lazy" width="800" height="540"
        />
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     THE FULL ECOSYSTEM
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner">
    <p class="eyebrow eyebrow-center">Tech Manager</p>
    <h2 class="h2-center">The smartest thing in your AV booth<br/>isn't your switcher.</h2>
    <p class="sub-center">
      Tech Manager is a free desktop app for Windows and Mac that runs silently on your production computer. It maps every device on your network, monitors your OBS health, documents your AV system, and feeds Luminos everything it needs to diagnose problems fast.
    </p>

    <div class="eco-grid">
      {[
        { label:"Monitoring", title:"OBS connection monitoring", desc:"Connection status, health checks, and log visibility before your congregation notices a problem.", href:"/tech-manager", image:`${R2NEW}/Stream%20Health.jpg`, alt:"WorshipMetrics stream health monitoring" },
        { label:"Discovery", title:"LAN device discovery", desc:"Every camera, encoder, and display on your network mapped automatically so knowledge survives turnover.", href:"/your-av-partner", image:`${R2}/homepage/Team%20Collaboration%20hero.jpg`, alt:"WorshipMetrics AV system discovery" },
        { label:"Documentation", title:"Visual wiring diagram", desc:"A living map of your AV signal chain so troubleshooting starts with clarity instead of guesswork.", href:"/your-av-partner", image:`${R2}/homepage/Church%20Viewer.jpg`, alt:"WorshipMetrics visual system map" },
        { label:"Resilience", title:"Local signage hosting", desc:"Lobby screens stay live even when internet drops because Tech Manager keeps the local layer ready.", href:"/signage-display", image:`${R2NEW}/Worship%20Digital%20Signage%20System.jpg`, alt:"WorshipMetrics signage on a display" },
        { label:"Knowledge", title:"AV system documentation", desc:"When something breaks on Sunday morning, Luminos already knows what is connected to what and where to start.", href:"/luminos-ai-assistant", image:`${R2}/homepage/Luminos.jpg`, alt:"Luminos AI assistant" },
        { label:"Free", title:"Windows and Mac app", desc:"Free to download. Works with or without the Hub.", href:"/tech-manager", badge:"Free", image:`${R2}/homepage/1%20Worship%20Technology%20Assistance.jpg`, alt:"Worship technology assistance" },
      ].map(({ label, title, desc, href, badge, image, alt }) => (
        <a href={href} class="eco-card">
          <div class="eco-card-media">
            <img src={image} alt={alt} loading="lazy" decoding="async" />
          </div>
          <div class="eco-label">{label}</div>
          <div class="eco-title">{title} {badge && <span class="eco-badge">{badge}</span>}</div>
          <div class="eco-desc">{desc}</div>
          <div class="eco-link">Learn more →</div>
        </a>
      ))}
    </div>
    <div style="display:flex;justify-content:center;gap:0.75rem;flex-wrap:wrap;margin-top:2rem;">
      <a href="/tech-manager" class="btn-primary">Download Tech Manager</a>
      <a href="/your-av-partner" class="btn-outline">Learn more</a>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     LUMINOS AI
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner-md">
    <div class="luminos-band">
      <div>
        <p class="luminos-tag">Luminos AI</p>
        <h3>Walk into Monday already knowing what matters most.</h3>
        <p>Luminos lives inside your WorshipMetrics dashboard and answers the questions your team asks every Monday — in plain English, without digging through reports. It reads attendance data, AV health logs, online engagement, and your follow-up queue, then tells you where to focus first.</p>
        <a href="/luminos-ai-assistant" class="btn-primary">See Luminos in action</a>
      </div>
      <div class="luminos-prompts">
        {[
          '"Who has missed multiple services and may need a check-in?"',
          '"Which guests still haven\'t heard from us this week?"',
          '"Give me a summary before Monday staff meeting."',
          '"What changed this week that deserves our attention first?"',
          '"My stream went down — what happened and how do I fix it?"',
        ].map(prompt => (
          <div class="luminos-prompt">{prompt}</div>
        ))}
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     HOW IT WORKS
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <p class="eyebrow eyebrow-center">The weekly rhythm</p>
    <h2 class="h2-center">Simple enough to repeat every service.</h2>
    <p class="sub-center">A four-step rhythm your team can repeat every week without rebuilding the process from scratch.</p>
    <ol class="steps">
      {[
        { n:"1", t:"Ready", b:"Tech Manager confirms every device is live. Stream is healthy. Audio is active. Displays are running. Your team sees green before the first person walks in." },
        { n:"2", t:"Count", b:"Auto-Count handles the room while your volunteers focus on people. Templates mean they're confirming changes, not recounting from zero." },
        { n:"3", t:"Capture", b:"Welcome cards scan. QR codes check in. Prayer requests route. Online viewers get a next step. All of it creates real people records." },
        { n:"4", t:"Reach", b:"Monday morning, Luminos surfaces who's new, who's missing, and what needs pastoral attention. Your team acts with intention — not reaction." },
      ].map(({ n, t, b }) => (
        <li class="step">
          <span class="step-num">{n}</span>
          <div class="step-body"><h3>{t}</h3><p>{b}</p></div>
        </li>
      ))}
    </ol>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     VOLUNTEER ROLES
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner">
    <div class="split">
      <div class="split-img">
        <img
          src={`${R2NEW}/Worship%20Volunteers%20Working%20Together.jpg`}
          alt="Worship volunteers working together using WorshipMetrics"
          loading="lazy" width="800" height="540"
        />
      </div>
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">Volunteer engagement</p>
        <h2>Every volunteer deserves a role that matters.</h2>
        <p>WorshipMetrics is built around the idea that technology should create opportunities to serve — not replace them. Every role gets tools built exactly for them, so volunteers are more deeply engaged, not just more efficient.</p>
        <div class="roles-grid" style="margin-top:1.25rem;">
          {[
            { emoji:"🙋", name:"Greeter",         job:"Welcome desk",      desc:"Check in guests and capture first-time visitors in seconds." },
            { emoji:"👶", name:"Children's Team",  job:"Kids ministry",     desc:"Run safe, fast family check-in tied to the same church record." },
            { emoji:"🎬", name:"AV Volunteer",     job:"Production booth",  desc:"Monitor streams, control cameras, and manage scenes remotely." },
            { emoji:"✝️", name:"Pastor / Leader",  job:"Leadership",        desc:"See who's new, who's missing, and what needs attention Monday." },
          ].map(({ emoji, name, job, desc }) => (
            <div class="role-card">
              <div class="role-emoji">{emoji}</div>
              <div class="role-name">{name}</div>
              <div class="role-job">{job}</div>
              <div class="role-desc">{desc}</div>
            </div>
          ))}
        </div>
        <a href="/volunteer-tools-for-congregations" class="btn-primary" style="margin-top:1.5rem;display:inline-flex;">See how roles work</a>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     ORIGIN / MISSION
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner-md">
    <div class="mission-band">
      <h2>Built by people who believe in the mission.</h2>
      <p>
        WorshipMetrics was built by Rev. Andrew Esqueda and Paul Richards — a worship leader and a
        broadcast technologist — because they lived these problems firsthand.
      </p>
      <p style="color:rgba(255,255,255,0.6);font-size:0.9rem;max-width:38rem;margin:0 auto 2rem;">
        The question that started it all: why should a leader need a spreadsheet to know who showed up?
        With cameras already in the room, the answer was simple — they shouldn't. That question became this platform.
      </p>
      <p style="color:rgba(255,255,255,0.6);font-size:0.9rem;max-width:38rem;margin:0 auto 2rem;">
        We build with the conviction that every congregation — regardless of size or budget — deserves technology
        that respects the sacred work happening inside those walls.
      </p>
      <div class="founders">
        <div class="founder">
          <div class="founder-avatar">
            <img src={ANDREW_HEADSHOT} alt="Rev. Andrew Esqueda" loading="lazy"/>
          </div>
          <span class="founder-name">Rev. Andrew Esqueda</span>
          <span class="founder-title">Co-Founder · Worship Leader</span>
        </div>
        <div class="founder">
          <div class="founder-avatar">
            <img src={PAUL_HEADSHOT} alt="Paul Richards" loading="lazy"/>
          </div>
          <span class="founder-name">Paul Richards</span>
          <span class="founder-title">Co-Founder · Technology Lead</span>
        </div>
      </div>
      <a href="/about" class="btn-outline" style="border-color:rgba(255,255,255,0.3);color:#fff;">
        Read our story
      </a>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     TESTIMONIALS
════════════════════════════════════════════════════ -->
<section class="section">
  <div class="inner">
    <p class="eyebrow eyebrow-center">Testimonials</p>
    <h2 class="h2-center">What congregations notice first</h2>
    <div class="testimonial-grid">
      {[
        {
          quote: '"Before WorshipMetrics, we had no idea who was watching our stream. Now we have a follow-up system running within 24 hours of every service."',
          author: "Executive Pastor, mid-size congregation, Southeast U.S.",
        },
        {
          quote: '"The AV monitoring piece changed everything. We used to find out the stream was down when someone texted us. Now we know before anyone else does."',
          author: "AV Director, growing congregation, Midwest",
        },
        {
          quote: '"Our YouTube chat used to go unmonitored every service. Now someone is in there welcoming viewers and routing prayer requests in real time."',
          author: "Communications Director, large congregation, Texas",
        },
      ].map(({ quote, author }) => (
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-quote">{quote}</p>
          <p class="testimonial-author">— {author}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     KNOWLEDGE BASE
════════════════════════════════════════════════════ -->
<section class="section section-alt">
  <div class="inner">
    <div class="split">
      <div class="split-text">
        <p class="eyebrow" style="margin-bottom:0.5rem;">Knowledge Base</p>
        <h2>Answers to the church AV questions teams ask every week.</h2>
        <p>Our AV Knowledge Base is built for production directors, booth volunteers, and technical teams who need practical help with streaming, PTZ cameras, OBS, signage, audio, and device management.</p>
        <ul class="bullets">
          <li>20 starter guides covering the most common church AV and operations questions</li>
          <li>Categories for streaming, audio, displays, device management, video clipping, and operations</li>
          <li>Built as a searchable editorial resource, not a generic blog</li>
          <li>Designed to help teams solve real Sunday problems faster</li>
        </ul>
        <div style="display:flex;flex-wrap:wrap;gap:0.75rem;">
          <a href="/kb" class="btn-primary">Explore the knowledge base</a>
          <a href="/kb/search" class="btn-outline">Search articles</a>
        </div>
      </div>
      <div class="split-img">
        <img
          src={`${R2NEW}/Stream%20Health.jpg`}
          alt="Church AV knowledge base and monitoring topics"
          loading="lazy" width="800" height="540"
        />
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════
     CTA
════════════════════════════════════════════════════ -->
<div class="cta-band">
  <p class="eyebrow" style="color:rgba(255,255,255,0.6);margin-bottom:0.5rem;">Every person. Every screen. Every service.</p>
  <h2>Every person.<br/>Every screen. Every service.</h2>
  <p>
    Sign up in two minutes. Get your free metrics dashboard, your free signage URL, and a clear path to automating everything else when you're ready.
  </p>
  <div class="cta-btns">
    <a href={`${APP_URL}/sign-up`} class="btn-white">Start free — no credit card</a>
    <a href="/appliance" class="btn-ghost-white">See the Hub — $499</a>
  </div>
  <p class="cta-note">Available Monday–Friday · We respond within one business day</p>
</div>

</Layout>
